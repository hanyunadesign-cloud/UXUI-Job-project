// 아이콘 크기 3단계(16/20/24px). 타이포그래피(text-h1~text-caption)는 globals.css의
// @layer components에 정의되어 있고, 간격 규칙은 같은 파일 상단 주석에 문서화되어 있다.
// 아바타·회사 로고 같은 이미지 컨테이너 크기는 이 스케일과 무관하게 각 컴포넌트에서 별도로 정한다.
export const ICON_SIZE = {
  sm: "h-4 w-4", // 16px - 기본 인라인 아이콘(내비게이션, 버튼, 필터)
  md: "h-5 w-5", // 20px - 강조가 필요한 아이콘
  lg: "h-6 w-6", // 24px - 단독으로 쓰이는 주요 액션 아이콘
  // 36px는 인터랙션 아이콘이 아니라 빈 상태/완료 화면 등에서 단독으로 크게 보여주는
  // 일러스트레이션성 아이콘 전용. 위 3단계와 섞어 쓰지 않는다.
  xl: "h-9 w-9",
} as const;
